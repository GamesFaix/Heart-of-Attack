using UnityEngine;


public class GUILobbyNetwork : MonoBehaviour {
	/*
	NetworkConsole nc;
	string server = "192.168.";
	
	
	void Awake () {
		nc = gameObject.GetComponent("NetworkConsole") as NetworkConsole;
	}
	
	public void Display (Panel p) {
		GUI.Box(p.FullBox,"");
		
		float btnW = 0.5f;
		//network stuff
		if (GUI.Button(p.Box(btnW), "Host")) {nc.Host();}

		if (GUI.Button(p.Box(btnW), "Join:")) {nc.Join(server);}

		p.NextLine();
		p.x2 += p.W/2;
		server = GUI.TextField(p.Box(btnW), server, 15);

		p.NextLine();
		p.y2 += 5;
		p.x2 += 5;
		GUI.Label(p.Box(0.8f), "Network status: "+nc.Status());
	}
	
	*/
}
